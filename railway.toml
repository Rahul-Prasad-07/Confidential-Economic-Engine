[build]
builder = "NIXPACKS"
buildCommand = "cd app && yarn install --frozen-lockfile && yarn build"

[deploy]
startCommand = "cd app && yarn start -p ${PORT} -H 0.0.0.0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
